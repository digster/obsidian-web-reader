# Root Environment Configuration for Docker Compose
# Copy this file to .env and adjust values as needed

# ===========================================
# REQUIRED SETTINGS
# ===========================================

# Password for accessing the web reader (REQUIRED)
APP_PASSWORD=your-secure-password-here

# Path to your Obsidian vaults directory on the host machine (REQUIRED for production)
# This should be the parent directory containing your vault folders
VAULTS_PATH=/path/to/your/vaults

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# Secret key for JWT token signing (auto-generated if not set)
# SECRET_KEY=your-secret-key-here

# Environment mode: development or production
# ENV=production

# Debug mode (set to false in production)
# DEBUG=false
