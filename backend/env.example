# Backend Environment Configuration
# Copy this file to .env and adjust values as needed

# ===========================================
# REQUIRED SETTINGS
# ===========================================

# Password for accessing the web reader (REQUIRED)
APP_PASSWORD=your-secure-password-here

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# Secret key for JWT token signing (auto-generated if not set)
# SECRET_KEY=your-secret-key-here

# Environment mode: development or production
ENV=development

# Debug mode (set to false in production)
DEBUG=true

# Server host and port
HOST=0.0.0.0
PORT=8000

# ===========================================
# AUTHENTICATION
# ===========================================

# JWT token expiration time in minutes (default: 1440 = 24 hours)
# ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Set cookies with Secure flag (default: false)
# Only set to true if you're serving the app directly over HTTPS
# If using a reverse proxy for HTTPS (nginx, traefik, etc.), leave this as false
# SECURE_COOKIES=false

# ===========================================
# FILE PATHS
# ===========================================

# Path to vaults configuration file
VAULTS_CONFIG=./vaults.json

# Data directory for search indexes and app data
DATA_DIR=./data

# ===========================================
# CORS SETTINGS (Development Only)
# ===========================================

# Allowed CORS origins (JSON array format)
# Required when frontend runs on different port/domain
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

